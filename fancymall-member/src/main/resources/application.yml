spring:
  datasource:
    username: root
    password: mysql
    url: jdbc:mysql://localhost:3306/fancymall_ums
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto #auto increment
