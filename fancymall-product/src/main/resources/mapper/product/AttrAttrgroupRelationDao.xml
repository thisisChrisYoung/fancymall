<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.chrisspace.fancymall.product.dao.AttrAttrgroupRelationDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.chrisspace.fancymall.product.entity.AttrAttrgroupRelationEntity" id="attrAttrgroupRelationMap">
        <result property="id" column="id"/>
        <result property="attrId" column="attr_id"/>
        <result property="attrGroupId" column="attr_group_id"/>
        <result property="attrSort" column="attr_sort"/>
    </resultMap>
    <update id="updateGroupIdByAttrId">
        UPDATE pms_attr_attrgroup_relation SET attr_group_id = #{attrGroupId}
        WHERE attr_id = #{attrId}

    </update>
    <delete id="deleteByAttrIdAndGroupId">
        delete from pms_attr_attrgroup_relation where
        <foreach collection="entities" item="item" separator=" OR ">
            (attr_id = #{item.attrId} and attr_group_id = #{item.attrGroupId})
        </foreach>
    </delete>

    <select id="queryAttrGroupIdByAttrId" resultType="java.lang.Long">
        SELECT attr_group_id FROM pms_attr_attrgroup_relation WHERE
        attr_id = #{attrId}
    </select>

    <select id="countGroupId" resultType="java.lang.Integer">
        SELECT count(1) FROM pms_attr_attrgroup_relation WHERE
        attt_id = #{attrId}
    </select>

    <select id="queryAttrIdsByGroupId" resultType="java.lang.Long">

        select attr_id from pms_attr_attrgroup_relation where
        attr_group_id = #{attrGroupId}
    </select>

    <select id="queryAttrsNotRelateByGroupId" resultType="java.lang.Long">
        select attr_id from pms_attr_attrgroup_relation where
        catelog_id = #{catelogId} and
        attr_group_id not in
        <foreach collection="attrGroupIds" separator="," item="item" open="(" close=")" index="index">
            #{item}
        </foreach>


    </select>


</mapper>